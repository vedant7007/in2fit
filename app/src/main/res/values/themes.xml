<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The window's chrome only; every colour and type on screen is ui/theme/Theme.kt (Ira).
         Light bars, so the status icons are dark on the cream ground. -->
    <style name="Theme.Katori" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowBackground">#ECEBE6</item>
    </style>
</resources>
