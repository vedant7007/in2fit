<?xml version="1.0" encoding="utf-8"?>
<!--
  Health data never leaves the device. Backup and device-transfer are both disabled outright
  rather than filtered, because a filter that misses a table leaks exactly the data spec 3.2
  says must stay local.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
